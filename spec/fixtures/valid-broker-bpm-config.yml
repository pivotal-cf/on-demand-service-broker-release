---
instance_groups:
  - name: broker
    jobs:
      - name: broker
        properties:
          service_adapter:
            mount_paths:
            - /var/vcap/path-to-some-config-directory
            - /var/vcap/path-to-some-other-directory
