---
instance_groups:
  - name: broker
    jobs:
      - name: broker
        properties:
          service_adapter:
            path: /var/vcap/custom/adapter/executable.sh
            mount_paths:
            - /var/vcap/path-to-some-config-directory
